description: Get all the Playlist Items
tags:
  - Playlist Items
produces:
  - "application/json"
security:
  - APIKeyHeader: [ 'Authorization' ]
get:
  description: Get all the playlist items.
responses:
  200:
    description: When all playlists are successfully obtained.

  400:
    description: Fails to list all playlists due to bad request data
